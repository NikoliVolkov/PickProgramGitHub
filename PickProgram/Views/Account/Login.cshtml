@model LoginViewModel
    @*if (ViewData.ModelState.Any(x => x.Value.Errors.Any()))
        {
            <div asp-validation-summary="All" class="alert alert-danger alert-dismissable"><a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a></div>
        }*@
    <div class="container">

        <form class="form-signin" asp-controller="Account" asp-action="Login" method="post">
            <h3 class="text-center">Please sign in.</h3>
            <div class="form-group">
                <div class="input-group">
                    <span class="input-group-addon"><label asp-for="@Model.Username"><span class="glyphicon glyphicon-user"></span></label></span>
                    <input asp-for="@Model.Username" type="text" class="form-control" placeholder="Username">
                </div>

                <div class="input-group">
                    <span class="input-group-addon"><label asp-for="@Model.Password"><span class="glyphicon glyphicon-lock"></span></label></span>
                    <input asp-for="@Model.Password" class="form-control" placeholder="Password">
                </div>
            </div>
            @*<div class="checkbox">
            <label>
                <input type="checkbox" value="remember-me"> Remember me
            </label>
        </div>*@
            <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
        </form>

    </div>
